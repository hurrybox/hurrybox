<section class="probootstrap-cover overflow-hidden relative"  style="background-color: #333; height: 100vh;" data-stellar-background-ratio="0.5"  id="section-home">
    <div class="overlay"></div>
        <div class="container">
            <div class="col-lg-6 offset-lg-3 col-md-12">
                <h1>Ανάκτηση κωδικού</h1>
                {{#if hasErrors}}
                    <div class="alert alert-danger">
                        {{#each messages }}
                            <p>{{this}}</p>
                        {{/each}}
                    </div>
                {{/if}}
                <form action="/users/forgot" method="post">
                    <div class="form-group">
                        <label for="email">E-mail</label>
                        <input type="email" id="email" name="email" class="form-control">
                    </div>
                    <button type="submit" class="btn btn-primary btn-block">Αποστολή Email</button>
                </form>
            </div>
        </div>
</section>